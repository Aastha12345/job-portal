<!DOCTYPE html>
<html>

  <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <meta http-equiv="X-UA-Compatible" content="ie=edge">
        
      <title>Header</title>
  </head>    

<body>

    <header style="background-color: #ece8e8b7;">
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          
          <% if(!currentUser){ %>
          <ul class="nav navbar-nav" style="float:right;">
            <li><a href='/login'><button class="btn btn-success">LOGIN</button></a></li>
            <li><a href='/register'><button class="btn btn-success">REGISTER</button></a></li>
          </ul>
          
          <% } else if(currentUser) { %>
          
            <li style="float:left;">
              <form action="/story/find" method='GET'><input type="text" name="searchstory" placeholder="Search Blogs">
              </form>
            </li>

            <% if(currentUser.category == "employer") {%>
              <button class="btn" type="button"><a href="/user/profiles">Search Candidates</a></button>
            <% } %>

            <div class="dropdown" style="float:right;">
              
              <button class="btn  dropdown-toggle" type="button" data-toggle="dropdown"><%= currentUser.username %>
              </button>
              
              <ul class="dropdown-menu">
                <li><a href='/user/profile/<%= currentUser._id %>'><i class="fa fa-user"></i>Profile</a></li>

                <% if(currentUser.category == "employer") {%>

                  <li><a href='/story/myblogs/<%= currentUser.username %>'><i class="fa fa-file"></i>Jobs Posted</a></li>

                <% } %>

                <li><a href='/user/logout'><i class="fa fa-power-off"></i>Logout</a></li>
              </ul>

            </div>
          
          <% } %>

        </div>
      </nav>
    </header>
<!--Body section is closed inside footer.ejs-->